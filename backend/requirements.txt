# Core API. Fast to install — use this for local development.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.6.0
python-multipart>=0.0.12
httpx>=0.27.0

# Auth + database
supabase>=2.10.0
pyjwt[crypto]>=2.9.0

# Integrations (light clients)
cloudinary>=1.41.0
firebase-admin>=6.6.0

# Utilities
# tzdata ships the IANA database. Windows has none at all, and slim Linux
# images often omit it - without this, ZoneInfo('Asia/Kuala_Lumpur') raises.
tzdata>=2024.1
python-dateutil>=2.9.0
tenacity>=9.0.0
