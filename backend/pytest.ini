[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
markers =
    slow: hits real OCR models; excluded by default (run with -m slow)
filterwarnings =
    ignore::DeprecationWarning
addopts = -m "not slow"
